apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: authservice-monitor
  labels:
    app: authservice
    release: prometheus
spec:
  selector:
    matchLabels:
      app: authservice
  endpoints:
    - port: 8081
      path: /actuator/prometheus
      interval: 30s
