apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: jwtproxy-monitor
  labels:
    app: jwtproxy
    release: prometheus
spec:
  selector:
    matchLabels:
      app: jwtproxy
  endpoints:
    - port: 8084
      path: /actuator/prometheus
      interval: 30s
