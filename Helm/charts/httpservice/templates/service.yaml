apiVersion: v1
kind: Service
metadata:
  name: httpservice
  labels:
    app: httpservice
spec:
  type: ClusterIP
  selector:
    app: httpservice
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
