apiVersion: v1
kind: Service
metadata:
  name: handleservice
  labels:
    app: handleservice
spec:
  type: ClusterIP
  ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
  selector:
    app: handleservice
