global:
  scrape_interval: 20s
scrape_configs:
  - job_name: 'prometheus_grafana'
    metrics_path: '/actuator/prometheus'
    static_configs:
      ###СТАТИЧЕСКИЕ РЕСУРСЫ -МЕТРИКИ(ВСЕГДА СТААТИЧНЫ)
      - targets: [ 'host.docker.internal:2009' ]